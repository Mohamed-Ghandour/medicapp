services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: medicapp
      POSTGRES_USER: medicapp
      POSTGRES_PASSWORD: medicapp
    ports:
      - "5433:5432"
    volumes:
      - medicapp_pgdata:/var/lib/postgresql/data

volumes:
  medicapp_pgdata:
